using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using Lean.Localization;

public class AvatarPopup : MonoBehaviour {
/*
    private string avatarPopup;

    private LevelPopupCreator avatarObj;

    public LeanLocalizedTextArgs AvatarName;
    public LeanLocalizedTextArgs AvatarPopulation;

    private void Start()
    {
        avatarPopup = transform.parent.name.Substring(6, 2);

        avatarObj = Resources.Load("Avatar " + avatarPopup) as LevelPopupCreator;

        Debug.Log(avatarObj.levelName);

        setAllText(AvatarName, avatarObj.levelName);
        AvatarPopulation.PhraseName =  avatarObj.population;
    }

    void setAllText(LeanLocalizedTextArgs leanLocalizedTextArgs, string text)
    {
        leanLocalizedTextArgs.PhraseName = text;
        leanLocalizedTextArgs.FallbackText = text;
        leanLocalizedTextArgs.GetComponent<Text>().text = text;
    }
    */
}
